// 10% Discount if amount is over 1k

using System;

class Program
{
    static void Main()
    {
        Console.WriteLine("Enter a number:");
        double number = double.Parse(Console.ReadLine());

        if (number > 1000)
        {
            number -= number * 0.10; // Reduce by 10%
        }
        Console.WriteLine("The final amount is: " + number);
    }
}
